spring:
  application:
    name: tc-eqp-socket-sim

  # ✅ 외부 설정 폴더 자동 로드
  # - 프로젝트 루트(또는 jar 실행 위치)의 ./config/application.yml 을 읽는다.
  # - 없으면 무시(optional)하고 내부 설정만 사용한다.
  config:
    import: "optional:file:./config/"

logging:
  level:
    root: INFO